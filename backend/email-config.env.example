# Add these email configuration variables to your .env file

# SMTP Configuration (Gmail example)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# Email Sender Info
FROM_NAME=Nyord Banking
FROM_EMAIL=noreply@nyordbank.com

# Note: 
# 1. For Gmail, use App Passwords (not your regular password)
# 2. Enable 2-factor authentication and generate an app password
# 3. Go to: Google Account > Security > 2-Step Verification > App passwords
# 4. Generate a password for "Mail" application
# 5. Use that generated password as SMTP_PASSWORD